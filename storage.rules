rules_version = "2";
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // לאמולטור: מאפשר למשתמשים מחוברים בלבד
      allow read, write: if request.auth != null;
    }
  }
}
